

describe("first render", () => {})

describe("render list", () => {})

describe("render a task", () => {
    expect(screen.getByRole("checkbox", { name: "Remove" })).toBeInTheDocument();
})


describe("task counter", () => {})
